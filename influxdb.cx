in package /driver/mnt/influxdb

class Mount: core/mount, hidden:/
    host: string
    db: string
    int16 construct()

    // App Publishes Data
    void onNotify(core/subscriberEvent event) override
    // App Requests Data (corto_select)
    core/resultIter onQuery(core/query query) override
